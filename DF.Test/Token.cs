using System;
using System.Collections.Generic;
using DF.BL;
using DF.Models;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace DF.Test
{
    [TestClass]
    public class Token
    {
        [TestMethod]
        public void TestToken()
        {
            //List<ClaimModel> claims = new List<ClaimModel>();
            //claims.Add(new ClaimModel() { Name = "claim-1", Value = "val-1" });
            //claims.Add(new ClaimModel() { Name = "claim-2", Value = "val-2" });

            //string token = TokenManager.CreateToken(claims);

            //Assert.IsTrue(!string.IsNullOrEmpty(token));

            //List<ClaimModel> claimsDecode = new List<ClaimModel>();

            //try
            //{
            //    claimsDecode = TokenManager.DecodeToken(token);
            //}
            //catch (Exception ex)
            //{

            //}

            //Assert.IsTrue(claimsDecode.Count > 0);
        }
    }
}
